name: rentapp
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  #  flutter_map: ^7.0.0  # Cập nhật lên phiên bản mới nhất
  #  latlong2: ^0.9.1     # Cập nhật để tương thích
  firebase_core: ^4.2.0
  cloud_firestore: ^6.0.3
  google_fonts: ^6.3.2
  firebase_auth: ^6.1.1
  firebase_database: ^12.0.3
  google_sign_in: ^6.3.0
  email_auth: ^2.0.0
#  firebase_storage: ^11.5.6
#  image_picker: ^1.0.4

  cupertino_icons: ^1.0.8
  flutter_map: ^6.1.0 # Sử dụng phiên bản mới nhất tương thích với Material 3
  latlong2: ^0.9.1
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1
  equatable: ^2.0.7
  get_it: ^8.2.0
  google_maps_flutter: ^2.13.1
  url_launcher: ^6.3.2
  geolocator: ^14.0.2
  intl: ^0.19.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  assets:
    - assets/

