name: motorbike_rental_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  #  flutter_map: ^7.0.0  # Cập nhật lên phiên bản mới nhất
  #  latlong2: ^0.9.1     # Cập nhật để tương thích
  firebase_core: ^4.2.0
  #  firebase_auth: ^4.15.3
  cloud_firestore: ^6.0.3
  google_fonts: ^6.3.2
  firebase_auth: ^6.1.1
  google_sign_in: ^6.3.0
  email_auth: ^2.0.0
#  firebase_storage: ^11.5.6
#  image_picker: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/